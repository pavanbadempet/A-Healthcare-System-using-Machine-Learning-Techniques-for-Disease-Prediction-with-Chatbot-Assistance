# Streamlit Secrets Template
# NOTE: Do NOT commit actual secrets to git. This is a template only.
# Copy this file to .streamlit/secrets.toml and add your actual values
# OR add these secrets in Streamlit Cloud dashboard under Settings > Secrets

# Backend API URL - REQUIRED for production deployment
BACKEND_URL = "https://aio-health-backend.onrender.com"

# Optional: Additional configuration
# GOOGLE_API_KEY = "your-key-here"  # Only if needed on frontend
